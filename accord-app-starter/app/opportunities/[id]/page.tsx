import Link from 'next/link';
import { AITimeline } from '@/components/AITimeline';
import { Shell } from '@/components/Shell';
import { mockOpportunity } from '@/lib/opportunity';
export default function OpportunityPage(){return <Shell active="drafts"><header className="topbar"><div><span className="eyebrow">Opportunity · Not yet in Deal Desk</span><h1>{mockOpportunity.title}</h1><p className="page-lead">Reconstructed from an agent recap. Evidence stays attributable until you approve it.</p></div><span className="status warn">Needs agent answers</span></header><div className="foundation-grid"><section className="card"><span className="section-kicker">Reconstructed work</span><h2>Draft preparation</h2><p>{mockOpportunity.reconstructedActivity?.summary}</p><div className="reconstruction-grid"><div><span>Suggested package</span><strong>{mockOpportunity.suggestedDraft}</strong></div><div><span>Deal Desk record</span><strong>Not created yet</strong></div></div><Link className="btn btn-primary" href="/transactions/txn-demo/review">Prepare Draft</Link></section><aside className="card"><span className="section-kicker">Ask only what is missing</span><h2>2 facts need attention</h2>{mockOpportunity.missingFactPrompts.map(f=><div className="missing-prompt" key={f.id}><strong>{f.question}</strong><small>{f.reason}</small></div>)}</aside></div><AITimeline events={mockOpportunity.timeline}/></Shell>}
